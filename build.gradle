plugins {
    id 'com.android.library' version '8.10.0' apply false
}

allprojects {
    group = rootProject.findProperty('GROUP') ?: 'com.github.naaammme'
    version = rootProject.findProperty('VERSION_NAME') ?: '1.0.0'
}
